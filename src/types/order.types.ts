export enum OrderStatus {
    PENDING = 'pending',
    IN_PROGRESS = 'in_progress',
    CHECKLIST_SUBMITTED = 'checklist_submitted',
    CONFIRMED = 'confirmed',
    COMPLETED = 'completed',
    REJECTED = 'rejected',
}
